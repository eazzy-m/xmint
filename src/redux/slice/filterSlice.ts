import {createSlice} from "@reduxjs/toolkit";

const initialState = {
    places: {

    },
    brands: {

    },
    athletes: {
        
    }
};

export const filters = createSlice({
    name: "filters",
    initialState,
    reducers: {
        
    }
});

export const {} = filters.actions;

// 