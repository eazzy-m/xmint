import {colors} from "../../constants/inlineConstants";

const {greyColor} = colors;

export const expandButtonStyle = {
    Width: "100%",
    textAlign: "left", 
    textTransform: "capitalize", 
    display: "flex", 
    justifyContent: "flex-start", 
    color: greyColor
};

export const formGroupStylesExpand = {height: "auto"};

export const formGroupStylesShrink = {height: "376px", overflow: "Hidden", width: "291px", flexWrap: "nowrap"};